[package]
name = "adblock_bridge"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
name = "reepax_adblock"

[dependencies]
adblock = { version = "0.13.3", default-features = true }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
